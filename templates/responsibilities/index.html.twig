{% extends 'layouts/app.html.twig' %}

{% block title %}{{ 'responsibilities'|trans({}, 'navigation') }} · {{ centre.name }} · {{ app_name }}{% endblock %}

{% block content %}
<div class="px-6 py-8 max-w-7xl mx-auto">

    <div class="mb-8">
        <h1 class="text-2xl font-bold text-gray-900">{{ 'responsibilities'|trans({}, 'navigation') }}</h1>
        <p class="mt-1 text-sm text-gray-500">{{ 'responsibilities.hub_intro'|trans({}, 'admin') }}</p>
    </div>

    <div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">

        {# ── Lists ── quality officer, management team and admins #}
        {% if is_granted('educational_centre.responsibilities', centre) %}
            <a href="{{ path('app_responsibilities_lists', {centreId: centre.id}) }}"
               class="group rounded-2xl border border-gray-200 bg-white shadow-xs overflow-hidden flex flex-col
                      hover:border-forest-200 hover:shadow-sm transition-all">
                <div class="flex items-center gap-4 px-6 py-5 border-b border-gray-100">
                    <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-forest-100 text-forest-600
                                group-hover:bg-forest-600 group-hover:text-white transition-colors">
                        {{ ux_icon('heroicons:list-bullet', {class: 'h-5 w-5'}) }}
                    </div>
                    <h2 class="text-base font-semibold text-gray-900">{{ 'responsibilities.lists.title'|trans({}, 'admin') }}</h2>
                </div>
                <div class="px-6 py-4 flex-1">
                    <p class="text-sm text-gray-500">{{ 'responsibilities.lists.hub_help'|trans({}, 'admin') }}</p>
                </div>
                <div class="px-6 py-3 bg-gray-50 border-t border-gray-100 flex items-center justify-end">
                    <span class="inline-flex items-center gap-1.5 text-sm font-medium text-forest-600 group-hover:text-forest-800 transition-colors">
                        {{ 'responsibilities.lists.manage'|trans({}, 'admin') }}
                        {{ ux_icon('heroicons:arrow-right', {class: 'h-4 w-4'}) }}
                    </span>
                </div>
            </a>
        {% endif %}

        {# ── Specific profiles ── quality officer, management team and admins #}
        {% if is_granted('educational_centre.responsibilities', centre) %}
            <a href="{{ path('app_responsibilities_specific_profiles', {centreId: centre.id}) }}"
               class="group rounded-2xl border border-gray-200 bg-white shadow-xs overflow-hidden flex flex-col
                      hover:border-forest-200 hover:shadow-sm transition-all">
                <div class="flex items-center gap-4 px-6 py-5 border-b border-gray-100">
                    <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-forest-100 text-forest-600
                                group-hover:bg-forest-600 group-hover:text-white transition-colors">
                        {{ ux_icon('heroicons:rectangle-group', {class: 'h-5 w-5'}) }}
                    </div>
                    <h2 class="text-base font-semibold text-gray-900">{{ 'responsibilities.specific_profiles.title'|trans({}, 'admin') }}</h2>
                </div>
                <div class="px-6 py-4 flex-1">
                    <p class="text-sm text-gray-500">{{ 'responsibilities.specific_profiles.hub_help'|trans({}, 'admin') }}</p>
                </div>
                <div class="px-6 py-3 bg-gray-50 border-t border-gray-100 flex items-center justify-end">
                    <span class="inline-flex items-center gap-1.5 text-sm font-medium text-forest-600 group-hover:text-forest-800 transition-colors">
                        {{ 'responsibilities.specific_profiles.manage'|trans({}, 'admin') }}
                        {{ ux_icon('heroicons:arrow-right', {class: 'h-4 w-4'}) }}
                    </span>
                </div>
            </a>
        {% endif %}

        {# ── Assign profiles ── quality officer, management team and admins #}
        {% if is_granted('educational_centre.responsibilities', centre) %}
            <a href="{{ path('app_responsibilities_assignments', {centreId: centre.id}) }}"
               class="group rounded-2xl border border-gray-200 bg-white shadow-xs overflow-hidden flex flex-col
                      hover:border-forest-200 hover:shadow-sm transition-all">
                <div class="flex items-center gap-4 px-6 py-5 border-b border-gray-100">
                    <div class="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-forest-100 text-forest-600
                                group-hover:bg-forest-600 group-hover:text-white transition-colors">
                        {{ ux_icon('heroicons:user-group', {class: 'h-5 w-5'}) }}
                    </div>
                    <h2 class="text-base font-semibold text-gray-900">{{ 'responsibilities.assignments.title'|trans({}, 'admin') }}</h2>
                </div>
                <div class="px-6 py-4 flex-1">
                    <p class="text-sm text-gray-500">{{ 'responsibilities.assignments.hub_help'|trans({}, 'admin') }}</p>
                </div>
                <div class="px-6 py-3 bg-gray-50 border-t border-gray-100 flex items-center justify-end">
                    <span class="inline-flex items-center gap-1.5 text-sm font-medium text-forest-600 group-hover:text-forest-800 transition-colors">
                        {{ 'responsibilities.assignments.manage'|trans({}, 'admin') }}
                        {{ ux_icon('heroicons:arrow-right', {class: 'h-4 w-4'}) }}
                    </span>
                </div>
            </a>
        {% endif %}

    </div>

</div>
{% endblock %}
